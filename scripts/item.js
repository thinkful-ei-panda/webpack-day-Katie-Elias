founction validateName(name) {
 if name = '' {
   return TypeError('Name must not be blank')
 };
}

function create(name) {
  let item {
    id: cuid,
    name: name,
    checked: false,
  }
}
export default {
  validateName();
  create();
};
